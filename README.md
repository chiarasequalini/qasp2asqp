# qasp2asqp
Translator from qasp planning problems to aspq planning problems 

# Usage
    $ qasp2aspq <file>
Where `<file>` is a planning problem/QBF formula in the qasp formalism. 

If a file name is not specified, the program reads from stdin. 